# Never copy the host's node_modules into the image — pnpm re-installs
# inside the container, and mixing platform-native binaries (Windows host
# vs. Linux container) crashes the resolver.
node_modules
dist
.vite
.playwright-cli
*.log
.env
.env.local
